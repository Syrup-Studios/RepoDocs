import type { RepoDocsConfig } from "@/lib/config";

const config: RepoDocsConfig = {
  site: {
    name: "RepoDocs",
    description: "Documentation that lives with the code.",
    repository: "https://github.com/Syrup-Studios/RepoDocs",
  },
};

export default config;
